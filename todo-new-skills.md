# Todo — new skills

## issue-management

- [ ] **Create an issue-management skill** — Codifies the GitHub issue lifecycle: creating issues (with type, title, body from a template), listing/filtering open issues, linking issues to branches or PRs, and closing issues on merge. Scope TBD: decide whether it's a companion to `git-feature` or a standalone skill covering the full issue-to-PR flow. Candidate modes: `new`, `list`, `link`, `close`.

- [ ] **Create a mode/skill for pulling origin main** — The git feature skill is missing an explicit section for `git pull origin main`. Consider adding a `pull` mode or skill.